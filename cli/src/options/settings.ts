import { CompositeInput } from "../option";

export class Settings extends CompositeInput {
  /**
   * These values don't matter - they will not be visible to the user
   */
  message = "Settings";
  code = "Settings";
  displayValue = "Settings";
}
